pipeline{

    stages{
        stage('Build hello-1 app'){
            steps{
                sh "docker version"
            }
        }
        stage('Build hello-2 app'){
            steps{

            }
        }
    }
}